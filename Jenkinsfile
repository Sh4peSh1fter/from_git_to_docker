pipeline {
    agent any

    stages {
        stage('run') {
            steps {
                echo 'running the python script'
                sh 'python3 main.py'
            }
        }
    }
}
